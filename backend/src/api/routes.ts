/**
 * routes.ts — barrel re-export.
 * All route logic lives in api/routes/*.routes.ts.
 * This file exists only to preserve the import path used by api/index.ts.
 */
export { default } from './routes/index.js';
